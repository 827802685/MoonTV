# MoonTV Cloudflare Pages 部署方案

## 当前状态

✅ 项目已使用 `@cloudflare/next-on-pages` 构建
✅ 构建输出位于 `.vercel/output/static/` (24MB, 108 个文件)
✅ `wrangler.toml` 已配置
❌ API Token 认证失败（可能需要重新生成）

---

## 部署方法

### 方法 A: 使用 Wrangler CLI (推荐)

**步骤 1: 登录 Cloudflare**

```bash
cd F:\Documents\2026\MoonTV
wrangler login
```

**步骤 2: 创建 D1 数据库**

1. 访问 https://dash.cloudflare.com
2. 进入 **存储和数据库 -> D1 SQL 数据库**
3. 点击 **创建数据库**
4. 名称: `moon-tv-db`
5. 记录生成的数据库 ID

**步骤 3: 更新 wrangler.toml**
将 `database_id = ""` 替换为实际的数据库 ID

**步骤 4: 初始化数据库**
在 D1 数据库的 **Explore Data** 页面运行：

```sql
-- 复制 D1初始化.md 的内容执行
```

**步骤 5: 添加环境变量**
在 Cloudflare Pages 控制台：

- Pages -> moon -> Settings -> Environment Variables
- 添加变量（类型选择 Secret）：

| 变量名                   | 值            |
| ------------------------ | ------------- |
| PASSWORD                 | your_password |
| USERNAME                 | admin         |
| NEXT_PUBLIC_STORAGE_TYPE | d1            |
| SITE_NAME                | MoonTV        |

**步骤 6: 部署**

```bash
wrangler pages deploy .vercel/output/static --project-name=moon --branch=main --no-bundle
```

---

### 方法 B: 手动上传到 Cloudflare Pages

**步骤 1: 访问 Cloudflare Pages**
https://dash.cloudflare.com -> Pages

**步骤 2: 选择项目**

- 项目名: `moon`
- 如果是新项目，点击 **创建项目**
- 如果是已有项目，进入项目设置

**步骤 3: 手动部署**

1. 进入 **部署** 标签页
2. 点击 **创建手动部署**
3. 选择 `.vercel/output` 目录（整个目录）
4. 点击 **保存并部署**

**步骤 4: 配置绑定**

1. 进入 **设置** -> **绑定**
2. 添加 **D1 数据库绑定**
   - 变量名称: `DB`
   - 数据库: `moon-tv-db`

**步骤 5: 配置环境变量**
进入 **设置** -> **环境变量**
添加以下 Secret 变量：

- PASSWORD=your_password
- USERNAME=admin
- NEXT_PUBLIC_STORAGE_TYPE=d1
- SITE_NAME=MoonTV

---

## 兼容性说明

本项目使用 `nodejs_compat` 标志，与 Cloudflare Workers/Pages 完全兼容。

**已测试的功能**:

- ✅ 静态页面渲染
- ✅ API 路由 (Edge Runtime)
- ✅ D1 数据库绑定
- ✅ 环境变量注入
- ✅ 中间件

---

## 故障排除

### 问题 1: 部署后 502 错误

**原因**: D1 数据库未绑定或 ID 不正确
**解决**:

1. 确认 `wrangler.toml` 中 `database_id` 正确
2. 在 Pages 设置中绑定 D1 数据库

### 问题 2: 数据库连接失败

**解决**: 确认数据库已初始化（运行 D1 初始化.md 中的 SQL）

### 问题 3: 登录失败

**解决**: 确认环境变量 PASSWORD 已设置

### 问题 4: 图片/资源加载失败

**解决**: 检查 config.json 中的资源站 API 地址是否可用（部分需要 HTTPS）

---

## 自定义域名 (可选)

1. 进入 Pages 项目 -> **自定义域名**
2. 添加域名: `moon.your-domain.com`
3. 在 DNS 设置中添加 CNAME 记录指向 `your-project.pages.dev`

---

## 相关文件

- `wrangler.toml` - Wrangler 配置
- `D1初始化.md` - 数据库表结构
- `config.json` - 资源站配置
- `.vercel/output/` - 构建输出
